name: to_do_bloc
description: A toDo list app using BloC pattern

version: 1.0.0+1

environment: 
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true